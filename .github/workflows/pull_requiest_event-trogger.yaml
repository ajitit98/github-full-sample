name: event trigger on pull request to specific branch 
on:
    push:
        branches:
            - main
    pull_request:
        types:
            opened
        branches:
            - main
jobs: 
    test: 
        runs-on: ubuntu-latest 
        steps: 
            - name: running deployment 
              run: echo "deployment succesful...."            
