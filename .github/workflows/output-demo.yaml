name: ouptut demo 
on:
    workflow_dispatch 
jobs: 
    job1:
        runs-on: ubuntu-latest 
        steps:
            - name: step one 
              id: step1
              run: echo "version=1.29.55" >> $GITHUB_OUTPUT 
            - name: stage two 
              run: echo "deployment version ${{steps.step1.outputs.version}}"      
 
   
       
        
        