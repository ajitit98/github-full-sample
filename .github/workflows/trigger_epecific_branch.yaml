name: push action trigger to specific branch 
on:
    push:
        branches: 
            - developer 
 
jobs: 
    test: 
        runs-on: ubuntu-latest 
        steps:
            - name: runninh test job 
              run: echo "running job to specific branch"        