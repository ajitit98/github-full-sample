name: env demo 
on: workflow_dispatch 
env:
    APP_ENV: "prod" 
jobs: 
    env_demo:
        runs-on: ubuntu-latest 
        steps: 
            - name: application run on environment 
              run: echo "Application is running on $APP_ENV"