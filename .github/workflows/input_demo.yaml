name: input demo 
on:
    workflow_dispatch:
        inputs: 
            environments:
                description: choose the environment to deploy 
                required: true 
                default: dev 
jobs: 
    input_demo:
        runs-on: ubuntu-latest 
        steps: 
            - name: deploy application to environment
              run: echo "Deploy application to ${{inputs.environments}}"

